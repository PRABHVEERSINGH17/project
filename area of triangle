#include<stdio.h>
#include<math.h>
int main(){
  float a,b,c,sp,area;
  printf("Enter the sides of the triangle:");
  scanf("%f %f %f",&a,&b,&c);
  sp=(a+b+c)/2;
  area=sqrt(sp*(sp-a)*(sp-b)*(sp-c));
printf("Area of the triangle is %f",area);
  return 0;
}
Enter the sides of the triangle:7
5
3
Area of the triangle is 6.495191 
