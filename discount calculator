/*While purchasing certain items , a discount of 10% is offered if the quantity purchase is more than 1000.If the quantity and price per items are input through the keyboard , write a program to calculate the total expenses.*/
#include<stdio.h>
int main(){
  int qty,dis;
  float rate,tot;
  printf("Enter the quantity and rate:");
  scanf("%d %f",&qty,&rate);
  if(qty>1000)
    dis=10;
  else
    dis=0;
  tot=(qty*rate)-(qty*rate*dis/100);
  printf("Total expenses=Rs.%f\n",tot);
  return 0;
}
Enter the quantity and rate:7
987
Total expenses=Rs.6909.000000
